# ./gradlew bootRun --args='--spring.profiles.active=dev'
# Compile templates from source on each change so edits show up on refresh.
gg.jte.development-mode=true
gg.jte.use-precompiled-templates=false
